import 'package:flutter/material.dart';

class WaitingData extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('loading data please wait...')),
    );
  }
}
